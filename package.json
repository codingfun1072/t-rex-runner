{
    "name": "t-rex-runner",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "inline": "npx inliner index.html > index.min.html"
    },
    "devDependencies": {
        "inliner": "^1.13.1"
    }
}
